import { View } from "react-native";
import styled from "styled-components/native";

/** the scenario clues sit on top of the description panel */
export const ScenarioContainer: typeof View = styled(View)`
  position: relative;
`;
