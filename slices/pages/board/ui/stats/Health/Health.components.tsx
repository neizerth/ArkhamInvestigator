import { Health } from "@shared/ui";
import { View } from "react-native";
import styled from "styled-components/native";

export const Container: typeof View = styled(View)`

`

export const Background: typeof Health = styled(Health)`
  height: 80px;
`