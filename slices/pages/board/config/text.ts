import { font } from "@shared/config"

const textSize = font.size.medium;

export const boardText = {
  ratio: {
    traits: 14 / textSize,
    text: 16 / textSize,
    icon: 14 / textSize,
    flavor: 14 / textSize,
  }
}