import type { ViewProps } from "react-native";
import type { ChaosTokenType } from "../../../model";

export type ChaosTokenInputProps = ViewProps & {
	type: ChaosTokenType;
};

export const ChaosTokenInput = (props: ChaosTokenInputProps) => {
	return <></>;
};
