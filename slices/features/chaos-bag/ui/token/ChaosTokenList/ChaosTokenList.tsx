import type { ChaosTokensCount } from "../../../model";

export type ChaosTokenListProps = {
	data?: ChaosTokensCount;
};

export const ChaosTokenList = (props: ChaosTokenListProps) => {
	return <></>;
};
