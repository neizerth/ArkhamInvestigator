import type { ViewProps } from "react-native";

export type ChaosTokenRevealModalContainerProps = ViewProps;

export const ChaosTokenRevealModalContainer = (
	props: ChaosTokenRevealModalContainerProps,
) => {
	return <></>;
};
