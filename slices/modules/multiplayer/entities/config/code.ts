/** Exactly 16 characters — each IP octet is encoded as two of them (high/low nibble).
 * Visually unambiguous glyphs only: no O/0, I/1, B/8, etc. */
export const codeAlphabet = "DFGJLQNRSUVWYZ47";

/** Query param of the https bridge link — must match `www/index.html`. */
export const JOIN_LINK_PARAM = "join";
