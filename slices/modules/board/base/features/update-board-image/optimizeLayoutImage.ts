import type { Box, BoxLayout } from "@shared/model";
import type { InvestigatorImage } from "arkham-investigator-data";

type Options = {
	image: InvestigatorImage;
	view: Box;
	layout: BoxLayout;
};
export const optimizeLayoutImage = (options: Options) => {};
