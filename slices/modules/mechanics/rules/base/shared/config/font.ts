import { font } from "@shared/config";

export const sectionTitleScale = 23 / 18;

export const phaseContentFontSize = font.size.small;

export const phaseTitleFontSize = phaseContentFontSize * sectionTitleScale;
